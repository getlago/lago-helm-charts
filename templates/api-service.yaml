apiVersion: v1
kind: Service
metadata:
  annotations:
  labels:
    io.lago.service: {{ .Release.Name }}-api
  name: {{.Release.Name}}-api-svc
spec:
  ports:
    - name: "3000"
      port: {{ .Values.api.port | default 3000 }}
      targetPort: 3000
  selector:
    io.lago.service: {{ .Release.Name }}-api
