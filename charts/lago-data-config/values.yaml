# Default values for lago-config.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  databaseAnalytical:
    host: ""
    name: ""
    user: ""
    password: ""
    port: 5432
    schema: analytical

  forecastedUsage:
    enabled: false
    # forecasted usage need to call lago-api, hence provide a valid token
    api_token:
    celery:
      brokerUrl: ""
      resultBackend: ""

  dbtPipeline:
    enabled: false
    sourceSchema: public
    targetSchema: analytical
    tables: []

  databaseReplica:
    host: ""
    name: ""
    user: ""
    password: ""
    port: 5432

  data:
    configmap:
    secret:
    # token to validate calls from lago-api
    token:


configmap:
  # -- Toggle creation of the ConfigMap. Set to false when using an externally managed ConfigMap
  # (provide its name via configmap.name, data.configmap.name, or global.data.configmap)
  create: true
  # -- Override the ConfigMap name. If not set and create is true, a name is generated using the fullname template
  name: ""

  # -- Labels to be added to lago-config configmap
  labels: {}
  # -- Annotations to be added to lago-config configmap
  annotations: {}

secret:
  # -- Create the lago-config secret for [sensitive config]
  create: true

  # The name of the secret to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

  # -- Labels to be added to lago-config secret
  labels: {}
  # -- Annotations to be added to lago-config secret
  annotations: {}
