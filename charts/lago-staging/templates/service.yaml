apiVersion: v1
kind: Service
metadata:
  name: {{ include "lago-staging.fullname" . }}
  labels:
    {{- include "lago-staging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lago-staging.selectorLabels" . | nindent 4 }}
