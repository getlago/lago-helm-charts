apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lago-front.fullname" . }}-test-connection"
  labels:
    {{- include "lago-front.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lago-front.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
