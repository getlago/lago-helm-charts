apiVersion: v1
kind: Service
metadata:
  name: {{ include "lago-data-api.fullname" . }}
  labels:
    {{- include "lago-data-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lago-data-api.selectorLabels" . | nindent 4 }}
