apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lago-data-api.fullname" . }}-test-connection"
  labels:
    {{- include "lago-data-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lago-data-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
