{{- if and .Values.gotenberg.autoscaling.enabled (not .Values.gotenberg.autoscaling.external) }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "lago-pdf.fullname" . }}
  labels:
    {{- include "lago-pdf.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "lago-pdf.fullname" . }}
  minReplicas: {{ .Values.gotenberg.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.gotenberg.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.gotenberg.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.gotenberg.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.gotenberg.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.gotenberg.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
