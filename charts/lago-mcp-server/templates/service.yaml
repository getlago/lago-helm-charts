apiVersion: v1
kind: Service
metadata:
  name: {{ include "lago-mcp-server.fullname" . }}
  labels:
    {{- include "lago-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lago-mcp-server.selectorLabels" . | nindent 4 }}
