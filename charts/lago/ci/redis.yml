apiVersion: v1
kind: Service
metadata:
  name: lago-redis
spec:
  selector:
    app: lago-redis
  ports:
    - port: 6379
      targetPort: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lago-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lago-redis
  template:
    metadata:
      labels:
        app: lago-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
