apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lago.fullname" . }}-test-connection"
  labels:
    {{- include "lago.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lago.fullname" . }}:{{ .Values.front.service.port }}']
  restartPolicy: Never
